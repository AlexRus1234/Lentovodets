# Планировочные документы агента — вне репозитория
/логи/

# Конфигурация agentic-инструментов — вне репозитория
AGENTS.md
opencode.json
.claude/
CLAUDE.md
.cursor/
.cursorrules
.github/copilot-instructions.md

# Legacy (старый nil-backup) — локальный референс, в git не попадает
/legacy/

# Go: бинари сборки и тестов
/lentovodec
/lentovodec.exe
*.exe~
*.test
/bin/
/dist/

# SQLite-каталог и логи (runtime artefacts)
*.db
*.db-shm
*.db-wal
*.log

# Локальные конфиги и секреты (пользователь может переопределять)
/lentovodec.toml
.env
.env.*

# Web-бандл, генерируемый Vite (_embed'ится в бинарь на этапе сборки);
# заглушку для сборки без фронтенда восстанавливает `make clean`
/internal/iface/web/assets/

# Web UI: зависимости npm (ставятся через make web-build)
/web/node_modules/

# Покрытие тестами
*.out
*.prof
/coverage/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Файлы ленточных "тренажёров" из filetape-тестов
*.tape
