# web

Исходники Vue 3 + Vite для Web UI. Создаются на **Этапе 8** ROADMAP.

Сборка (`make web-build`) кладёт бандл в `../internal/iface/web/assets/`,
откуда он встраивается в бинарь через `//go:embed`.
